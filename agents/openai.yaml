interface:
  display_name: "Agent Dispatch"
  short_description: "Fail-closed adaptive subagent routing and telemetry"
  default_prompt: "Use $agent-dispatch to route and verify substantive delegated work with pre-spawn receipts, append-only usage telemetry, and fail-closed completion audits."
